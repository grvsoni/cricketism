<!DOCTYPE html>
<html lang="en">
  <%= render "shared/head" %>
  <body>
    <%= render "shared/main_nav" %>

    <div class="container dropped-50">
        <div class="row">
          <div class="span3">
            <%= sidebar(current_user) %>
          </div><!--/span-->
          <div class="span9">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->

     <%= render "shared/footer" %>
     <%= render "shared/datatable_lib" %> 
     <%= render "shared/calender_lib" %>
    </div> <!-- /container -->

  </body>
</html>
