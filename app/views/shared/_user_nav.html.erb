<% if current_user.present? %>
  <ul class="nav pull-right">
    <li>
      <%= link_to "Welcome #{current_user.profile ? current_user.profile.fullname : current_user.username}", edit_user_path(current_user) %>
    </li>
    <li class="dropdown">
      <%= link_to "Signout", destroy_user_session_path, :method => :delete  %></li>
    </li>
  </ul>
<% end %>