<%= form_for @sponsor, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="row">
    <div class="span2">
      <div class="control-group">
        <%= f.label :logo, image_tag(@sponsor.logo.url(:large), :class => "img-thumbnail"), :class => 'control-label' %>
      </div>
    </div>
    <div class="span6">
      <%= render "shared/error_messages", :target => @sponsor %>
      <div class="control-group">
        <%= f.label :logo, "Sponsor Logo", :class => 'control-label' %>
        <div class="controls">
          <%= f.file_field :logo %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :name, :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :name, :class => 'text_field' %>
        </div>
      </div>
      <div class="control-group">
        <%= f.label :link, "Web Link", :class => 'control-label' %>
        <div class="controls">
          <%= f.text_field :link, :class => 'text_field' %>
        </div>
      </div>
      <%= f.hidden_field :user_id, :class => 'text_field', :value => current_user.id %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                sponsors_path, :class => 'btn' %>
  </div>
<% end %>
